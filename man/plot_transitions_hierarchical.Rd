% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggraph_transitions.R
\name{plot_transitions_hierarchical}
\alias{plot_transitions_hierarchical}
\title{Plot Employment Transitions - Hierarchical Layout}
\usage{
plot_transitions_hierarchical(
  transitions_data,
  input_format = "data.table",
  hierarchy_type = "sugiyama",
  root_nodes = NULL,
  layout_direction = "vertical",
  level_separation = 1,
  node_separation = 1,
  show_levels = TRUE,
  min_edge_weight = 1,
  node_size_var = "strength",
  edge_width_var = "weight",
  node_color_var = "level",
  node_size_range = c(3, 12),
  edge_width_range = c(0.5, 3),
  palette = "viridis",
  use_bw = FALSE,
  accessibility_mode = FALSE,
  use_consolidated_periods = TRUE,
  consolidation_type = "both",
  show_edge_labels = FALSE,
  show_node_labels = TRUE,
  label_angle = 0,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{transitions_data}{Data.table output from analyze_employment_transitions()}

\item{input_format}{Character. Format of input data: "data.table" or "matrix" (default: "data.table")}

\item{hierarchy_type}{Character. Type of hierarchical layout: "tree", "dendrogram", 
"sugiyama", "davidson_harel" (default: "sugiyama")}

\item{root_nodes}{Character vector. Nodes to use as roots (default: auto-detect)}

\item{layout_direction}{Character. Direction of hierarchy: "vertical", "horizontal" (default: "vertical")}

\item{level_separation}{Numeric. Separation between hierarchy levels (default: 1)}

\item{node_separation}{Numeric. Separation between nodes at same level (default: 1)}

\item{show_levels}{Logical. Highlight hierarchy levels with background colors (default: TRUE)}

\item{min_edge_weight}{Numeric. Minimum edge weight to display (default: 1)}

\item{node_size_var}{Character. Variable for node sizing (default: "strength")}

\item{edge_width_var}{Character. Variable for edge width (default: "weight")}

\item{node_color_var}{Character. Variable for node coloring (default: "level")}

\item{node_size_range}{Numeric vector. Size range for nodes (default: c(3, 12))}

\item{edge_width_range}{Numeric vector. Width range for edges (default: c(0.5, 3))}

\item{palette}{Character. Color palette (default: "viridis")}

\item{use_bw}{Logical. Use black and white version (default: FALSE)}

\item{accessibility_mode}{Logical. Enable accessibility mode (default: FALSE)}

\item{use_consolidated_periods}{Logical. Apply consolidation for cleaner hierarchy (default: TRUE)}

\item{consolidation_type}{Character. Type of consolidation (default: "both")}

\item{show_edge_labels}{Logical. Show edge weight labels (default: FALSE)}

\item{show_node_labels}{Logical. Show node labels (default: TRUE)}

\item{label_angle}{Numeric. Angle for node labels in degrees (default: 0)}

\item{title}{Character. Plot title (default: auto-generated)}

\item{subtitle}{Character. Plot subtitle (default: auto-generated)}
}
\value{
A ggplot2 object showing the hierarchical network
}
\description{
Creates hierarchical network visualizations using tree-like layouts.
Particularly effective for showing career progression, advancement patterns,
or directional flow between employment states.
}
\examples{
\dontrun{
# Basic consolidated hierarchical tree
plot_transitions_hierarchical(pipeline_result)

# Sugiyama layout with level highlighting, both consolidation types
plot_transitions_hierarchical(pipeline_result, hierarchy_type = "sugiyama", 
                             show_levels = TRUE, consolidation_type = "both")

# Horizontal dendrogram with no consolidation
plot_transitions_hierarchical(pipeline_result, hierarchy_type = "dendrogram", 
                             layout_direction = "horizontal", 
                             use_consolidated_periods = FALSE)
}
}
