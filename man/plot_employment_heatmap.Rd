% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vecshift_plots.R
\name{plot_employment_heatmap}
\alias{plot_employment_heatmap}
\title{Plot Employment Heatmap}
\usage{
plot_employment_heatmap(
  data,
  person_col = "cf",
  time_col = "inizio",
  end_col = "fine",
  status_col = "stato",
  heatmap_type = "density",
  time_unit = "month",
  group_by = NULL,
  facet_by = NULL,
  n_people = 50,
  palette = "main",
  use_bw = FALSE,
  show_values = FALSE,
  alpha = 0.9
)
}
\arguments{
\item{data}{Data.table output from vecshift() containing employment segments}

\item{person_col}{Character. Column name for person identifier (default: "cf")}

\item{time_col}{Character. Column name for time periods (default: "inizio")}

\item{end_col}{Character. Column name for period end dates (default: "fine")}

\item{status_col}{Character. Column name for employment status (default: "stato")}

\item{heatmap_type}{Character. Type of heatmap: "density", "status", "duration", "transitions" (default: "density")}

\item{time_unit}{Character. Time aggregation unit: "month", "quarter", "year" (default: "month")}

\item{group_by}{Character. Column to group by for comparison (default: NULL)}

\item{facet_by}{Character. Column to use for faceting (default: NULL)}

\item{n_people}{Integer. Maximum number of people to show (default: 50)}

\item{palette}{Character. Color palette to use (default: "main")}

\item{use_bw}{Logical. Use black and white palette (default: FALSE)}

\item{show_values}{Logical. Show values in heatmap cells (default: FALSE)}

\item{alpha}{Numeric. Transparency (default: 0.9)}
}
\value{
A ggplot2 object showing employment heatmap
}
\description{
Creates heatmap visualizations showing employment density patterns over time
and across populations, useful for identifying seasonal patterns, regional
differences, or temporal clustering of employment events.
}
\examples{
\dontrun{
# Employment density heatmap
plot_employment_heatmap(data, heatmap_type = "density")

# Status distribution heatmap
plot_employment_heatmap(data, heatmap_type = "status")

# Duration patterns by month
plot_employment_heatmap(data, heatmap_type = "duration", time_unit = "month")
}
}
