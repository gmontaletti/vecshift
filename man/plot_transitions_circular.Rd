% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggraph_transitions.R
\name{plot_transitions_circular}
\alias{plot_transitions_circular}
\title{Plot Employment Transitions - Circular Layout}
\usage{
plot_transitions_circular(
  transitions_data,
  input_format = "data.table",
  circular_type = "chord",
  node_order = "frequency",
  manual_order = NULL,
  show_flow_direction = TRUE,
  edge_curve = 0.1,
  min_edge_weight = 1,
  node_size_var = "strength",
  node_size_range = c(4, 20),
  edge_width_range = c(0.5, 5),
  palette = "viridis",
  use_bw = FALSE,
  accessibility_mode = FALSE,
  use_consolidated_periods = TRUE,
  consolidation_type = "both",
  show_percentages = FALSE,
  label_distance = 1.1,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{transitions_data}{Data.table output from analyze_employment_transitions()}

\item{input_format}{Character. Format of input data: "data.table" or "matrix" (default: "data.table")}

\item{circular_type}{Character. Type of circular layout: "chord", "arc", "wheel" (default: "chord")}

\item{node_order}{Character. How to order nodes: "alphabetical", "frequency", 
"cluster", "manual" (default: "frequency")}

\item{manual_order}{Character vector. Manual node order (when node_order = "manual")}

\item{show_flow_direction}{Logical. Show directional arrows for flows (default: TRUE)}

\item{edge_curve}{Numeric. Curvature for edges (0 = straight, 1 = very curved) (default: 0.1)}

\item{min_edge_weight}{Numeric. Minimum edge weight to display (default: 1)}

\item{node_size_var}{Character. Variable for node sizing (default: "strength")}

\item{node_size_range}{Numeric vector. Size range for nodes (default: c(4, 20))}

\item{edge_width_range}{Numeric vector. Width range for edges (default: c(0.5, 5))}

\item{palette}{Character. Color palette (default: "viridis")}

\item{use_bw}{Logical. Use black and white version (default: FALSE)}

\item{accessibility_mode}{Logical. Enable accessibility mode (default: FALSE)}

\item{use_consolidated_periods}{Logical. Apply consolidation for cleaner circular layout (default: TRUE)}

\item{consolidation_type}{Character. Type of consolidation (default: "both")}

\item{show_percentages}{Logical. Show edge percentages instead of raw counts (default: FALSE)}

\item{label_distance}{Numeric. Distance of labels from circle (default: 1.1)}

\item{title}{Character. Plot title (default: auto-generated)}

\item{subtitle}{Character. Plot subtitle (default: auto-generated)}
}
\value{
A ggplot2 object showing the circular network
}
\description{
Creates circular network layouts optimized for employment transition analysis.
Arranges nodes in a circle with transitions shown as chords or arcs.
Particularly effective for showing flow patterns and state relationships.
}
\examples{
\dontrun{
# Basic consolidated circular chord diagram
plot_transitions_circular(pipeline_result)

# Arc diagram with frequency-based ordering, overlapping consolidation only
plot_transitions_circular(pipeline_result, circular_type = "arc", node_order = "frequency",
                         consolidation_type = "overlapping")

# Wheel layout with percentages, no consolidation
plot_transitions_circular(pipeline_result, circular_type = "wheel", show_percentages = TRUE,
                         use_consolidated_periods = FALSE)
}
}
